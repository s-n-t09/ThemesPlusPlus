{
  "name": "themes-plus-plus",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check scripts/build.mjs"
  },
  "devDependencies": {
    "@swc/core": "^1.16.1",
    "esbuild": "^0.25.0"
  }
}
